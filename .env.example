# Optional LLM service configuration for future integrations.
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=

# Platform API defaults.
PORT=3001
DATABASE_URL="file:./dev.db"

# Optional storage overrides. Current code mainly uses local project storage.
DOCUMENT_STORAGE_PATH=
REPORT_OUTPUT_PATH=
PLM_API_BASE_URL=
QMS_API_BASE_URL=
